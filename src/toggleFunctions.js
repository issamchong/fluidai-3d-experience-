
  
  export const toggleRespiratory = (showRespiratory, setShowRespiratory) => {
    setShowRespiratory(!showRespiratory);
  };
  
  
  export const toggleDigestive = (showDigestive, setShowDigestive) => {
    setShowDigestive(!showDigestive);
  };
  
  export const toggleBrain = (showBrain, setShowBrain) => {
    setShowBrain(!showBrain);
  };
  
  export const toggleMuscles = (showMuscles, setShowMuscles) => {
    setShowMuscles(!showMuscles);
  };
  
  export const toggleSkin = (showSkin, setShowSkin) => {
    setShowSkin(!showSkin);
  };
